apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpsapp.fullname" . }}
  labels:
    {{- include "rpsapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rpsapp.selectorLabels" . | nindent 4 }}
