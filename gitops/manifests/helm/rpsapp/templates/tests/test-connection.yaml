apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rpsapp.fullname" . }}-test-connection"
  labels:
    {{- include "rpsapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rpsapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
